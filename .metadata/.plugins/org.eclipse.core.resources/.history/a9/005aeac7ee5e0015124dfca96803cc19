package BoxOffice.ServiceStrategy;

import java.util.ArrayList;

import BoxOffice.defaul.Passenger;

public class Strategy_LTFO implements Strategy{

	@Override
	public int getNextIndex(ArrayList<Passenger> passengers) {
		int index = 0;
		for(int i = 0; i < passengers.size(); i++){
			Passenger p = passengers.get(index);
			if(p.getTicketingTime() > passenger.get)
		}
		return 0;
	}
	
}