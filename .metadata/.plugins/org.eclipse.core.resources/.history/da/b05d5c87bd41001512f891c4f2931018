package Maze;

public class Strategy8 implements Strategy{

	@Override
	public void getNextDir(Point curPos) {
		switch (curPos.dir){
		case 0: 
			curPos.y++;
			curPos.dir++;
			return curPos;
		case
		}
		
	}

}
