package Maze;

import java.util.ArrayList;

public class Strategy8 implements Strategy{

	@Override
	public Point getNextDir(Point curPos) {
		if(curPos.dir < 8)
			curPos.dir++;
		
		else
			curPos.dir = -1;
	}

}
